# Expo SDK

This app targets Expo SDK 54 for compatibility with the current Play Store version of Expo Go.
Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before changing Expo APIs or dependencies.
